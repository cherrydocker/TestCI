sudo:
     true
language:
     python
python:
      - "2.7"
before_install:
      - "sudo yum install -y pip"
install:
      - "pip install tornado"

script:
      - "python test.py"
      - "python toranado_example.py"
